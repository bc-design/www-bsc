<!doctype html>
<head>
	<title>Test</title> 
	<meta charset=utf-8> 

	<script>
	function loadXMLDoc()
	{
	if (window.XMLHttpRequest)
	  {// code for IE7+, Firefox, Chrome, Opera, Safari
	  xmlhttp=new XMLHttpRequest();
	  }
	else
	  {// code for IE6, IE5
	  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
	  }
	xmlhttp.onreadystatechange=function()
	  {
	  if (xmlhttp.readyState==4 && xmlhttp.status==200)
		{
		document.getElementById("myDiv").innerHTML=xmlhttp.responseText;
		}
	  }
	xmlhttp.open("GET","status_info.txt",true);
	xmlhttp.send();
	}
	</script>
</head>

<body>
	<p><a href="/my-link/"><button>Click me</button></a>
	<p><a href="/journals/"><button>Journal List</button></a> - note that this will take a minute!

	<form action="." method="POST">
		<input type="text" name="text" size="50" style="height:100px;">
		<input type="submit" name="my-form" value="Send">
	</form>
	
</body>

